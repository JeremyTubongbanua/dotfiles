-- doing :h rtp shows that lua/ is a directory that searches for lua modules (rtp == runtimepath)
require("config.options")
require("config.keymaps")
require("config.commands")
require("config.lazy")

